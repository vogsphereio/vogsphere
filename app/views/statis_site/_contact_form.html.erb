<!--Section: Contact v.2-->
<div><%= form_with(model:@contact) do |f| %>
   <%= f.label :name %>
   <%= f.text_field :name %>
   <%= f.label :email %>
   <%= f.text_field :email %>
  <%= f.submit "Send" %>
<% end %></div>